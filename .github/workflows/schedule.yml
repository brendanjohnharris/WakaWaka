name: Update gist with WakaTime summary
on: [push]
jobs:
  update-gist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Update gist
        uses: superman66/wakatime-sync@master
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN ***REMOVED******REMOVED***
          GIST_ID: ${{ secrets.GIST_ID***REMOVED******REMOVED***
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY ***REMOVED******REMOVED***
